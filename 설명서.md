# 쇼핑몰 상품 크롤러 — 처음 보는 사람을 위한 설명서

코딩을 몰라도 이 프로그램이 뭘 하는지, 어떻게 실행하는지, 결과물이 뭔지
이해할 수 있도록 쓴 안내서입니다. 짧은 요약은 [`README.md`](./README.md)를
참고하세요.

---

## 1. 이게 뭔가요?

**"크롤러"(또는 "스크래퍼")**란 사람이 웹사이트를 하나하나 열어보면서
정보를 복사-붙여넣기 하는 작업을, 프로그램이 대신 자동으로 해주는
도구입니다. 이 프로젝트는 `webscraper.io`라는 곳에서 **크롤링 연습용으로
공개해 둔 가상의 전자제품 쇼핑몰**에서 노트북, 태블릿, 스마트폰 상품의
이름, 가격, 평점, 리뷰 수를 자동으로 긁어와 엑셀 파일로 정리해주는
프로그램입니다.

실제 외주에서는 이 구조를 그대로 활용해서, 예를 들어 "경쟁사 쇼핑몰의
상품 가격을 카테고리별로 자동으로 모아서 엑셀로 보내주는" 가격 비교
서비스 같은 걸 만들 수 있습니다.

## 2. 파일이 뭐가 있나요?

```
web-crawler-books/
├── scraper.py       ← 크롤링 + 저장 로직 (카테고리 기반, 실행 시작점)
├── ai_extractor.py  ← 셀렉터 없는 사이트를 AI(Gemini)로 대신 읽어주는 보조 기능
├── .env.example     ← AI 기능에 필요한 API 키 설정 예시
├── README.md        ← 프로젝트 요약
├── 설명서.md         ← 지금 보고 있는 이 문서
└── .gitignore       ← 실행 결과 파일/비밀 키 파일은 저장소에 올리지 않도록 제외 설정
```

실행하면 `output_products.csv`, `output_products.xlsx`라는 결과 파일이
새로 생기는데, 이 파일들은 저장소에는 포함되어 있지 않고 실행할 때마다
새로 만들어집니다.

## 3. 어떻게 실행하나요?

이 프로그램은 파이썬(Python)으로 만들어져 있어서, 더블클릭으로 바로 열리는
`index.html`과 달리 **컴퓨터에 파이썬이 설치되어 있어야** 실행할 수
있습니다.

1. 파이썬이 설치돼 있는지 확인 (없으면 python.org에서 설치)
2. 아래 명령어로 필요한 부품(라이브러리) 설치 (한 번만 하면 됨)
   ```
   pip install requests beautifulsoup4 xlsxwriter
   ```
3. 이 폴더 위치에서 아래 명령어 실행
   ```
   python scraper.py
   ```
4. 실행이 끝나면 같은 폴더에 `output_products.csv`, `output_products.xlsx`
   파일이 생성됨 — 엑셀로 열어서 바로 확인 가능

특정 카테고리만 수집하고 싶으면 이렇게 실행합니다.
```
python scraper.py --categories 노트북,태블릿
```

## 4. 실행하면 내부에서 무슨 일이 일어나나요? (쉬운 설명)

사람이 직접 한다면 이런 순서로 하게 될 작업을, 프로그램이 몇 초 만에
대신 해줍니다.

1. "노트북", "태블릿", "스마트폰" 카테고리를 하나씩 순서대로 처리
2. 각 카테고리 화면을 인터넷에서 가져옴 (이 쇼핑몰은 한 화면 안에
   해당 카테고리 상품이 전부 나와 있어서, 페이지를 여러 장 넘길 필요가
   없습니다 — 카테고리당 딱 1번만 화면을 가져오면 끝)
3. 그 화면 안에서 상품 하나하나의 "이름", "가격", "평점", "리뷰 수"가
   적힌 부분만 정확히 찾아서 뽑아냄
4. 세 카테고리를 다 처리하면, 모아둔 정보를 CSV 파일(메모장으로도 열리는
   단순 표 형식)과 Excel 파일(색깔·굵은 글씨 등 서식이 적용된 표) 두
   가지로 저장

> 참고: 원래는 `books.toscrape.com`이라는 가짜 서점 사이트를 대상으로
> 만들었는데, "실제 쇼핑몰/가격비교"에 더 가까운 예시로 바꿔서 지금의
> 전자제품 쇼핑몰 버전으로 다시 만들었습니다.

## 5. 결과물(엑셀 파일)은 어떻게 생겼나요?

| 카테고리 | 상품명 | 가격(USD) | 평점 | 리뷰 수 |
|---|---|---|---|---|
| 노트북 | Asus VivoBook X441NA-GA190 | 295.99 | 3 | 14 |
| 태블릿 | Lenovo IdeaTab | 69.99 | 3 | 7 |
| 스마트폰 | Nokia 123 | 24.99 | 3 | 11 |

- 맨 위 줄(제목 줄)은 진한 배경색으로 강조되어 있고, 스크롤해도 항상
  보이도록 고정되어 있습니다
- 가격 열은 통화 기호($)가 자동으로 붙는 서식이 적용돼 있습니다
- 카테고리별로 몇 개씩 모였는지는 엑셀의 필터 기능으로 바로 확인 가능

## 6. 실행 옵션 바꾸는 법

명령어 뒤에 옵션을 붙여서 동작을 조절할 수 있습니다.

| 옵션 | 뜻 | 예시 |
|---|---|---|
| `--categories` | 어떤 카테고리를 수집할지, 쉼표로 구분 (기본: 전체) | `python scraper.py --categories 노트북` |
| `--csv` | CSV 파일 저장 경로/이름 지정 | `python scraper.py --csv 결과.csv` |
| `--excel` | Excel 파일 저장 경로/이름 지정 | `python scraper.py --excel 결과.xlsx` |

## 7. 다른 사이트에도 이 프로그램을 쓸 수 있나요?

네, 하지만 **사이트마다 화면 구조가 달라서 코드를 그 사이트에 맞게 고쳐야
합니다.** 지금 코드는 이 특정 쇼핑몰의 화면 구조(어느 위치에 상품명이
있고, 어느 위치에 가격이 있는지)에 맞춰져 있습니다. 실제 클라이언트의
사이트에 쓰려면 그 사이트 구조를 분석해서 같은 방식으로 다시 맞춰야
합니다.

⚠️ **크롤링하기 전에 반드시 확인할 것**: 아무 사이트나 마음대로
크롤링하면 안 됩니다. 대상 사이트의 이용약관이나 `robots.txt`(크롤링
허용 범위를 적어둔 파일)를 먼저 확인하고, 로그인이 필요한 개인정보나
저작권이 있는 콘텐츠는 수집 대상에서 제외해야 합니다.

## 8. 셀렉터를 안 만들어도 AI가 대신 읽어주는 기능

지금까지 설명한 방식은 "이 사이트의 상품명은 여기, 가격은 여기"라고
사람이 미리 정확한 위치를 알려줘야만 동작합니다. 그런데 **아직 위치를
알려주지 않은, 처음 보는 사이트**를 급하게 시도해봐야 할 때도 있습니다.
이럴 때 쓰는 게 `--ai-url` 기능입니다.

동작 방식은 이렇습니다.
1. 지정한 페이지의 내용을 가져와서 사람이 읽는 것과 비슷한 순수 텍스트로 정리
2. 그 텍스트를 구글의 AI(Gemini)에게 보여주면서 "여기서 상품명, 가격,
   평점, 리뷰 수를 찾아서 정리해줘"라고 요청
3. AI가 정리해서 돌려준 결과를 기존과 똑같은 엑셀/CSV 형식으로 저장

사용하려면 먼저 무료 AI 키를 준비해야 합니다.
1. [aistudio.google.com](https://aistudio.google.com)에서 구글 계정으로 로그인
2. "API 키 만들기"로 무료 키 발급 (신용카드 등록 불필요)
3. `.env.example` 파일을 복사해서 `.env`라는 이름으로 저장하고, 그 안에
   발급받은 키를 붙여넣기 (이 파일은 나만 보는 파일이라 GitHub에는 절대
   올라가지 않습니다)
4. 아래처럼 실행
   ```
   python scraper.py --ai-url "https://대상사이트.com/상품목록"
   ```

⚠️ **이 방식의 한계**: AI는 화면에 "글자로 보이는 정보"만 읽을 수
있습니다. 예를 들어 별점이 숫자 텍스트가 아니라 ★★★☆☆ 같은 그림(아이콘)
으로만 표시된 사이트는, AI가 그 화면을 텍스트로 바꿔서 볼 때 별점 정보
자체가 사라져서 읽어내지 못합니다. 실제로 테스트해보니 상품명·가격·리뷰
수는 정확히 가져왔지만, 아이콘으로 된 별점은 전부 0으로 나왔습니다.
반면 처음부터 위치를 정확히 알려준 기존 방식(`scrape_products`)은
화면에 안 보이는 숨겨진 정보(`data-rating` 값)까지 읽을 수 있어서 더
정확합니다. 즉, **AI 방식은 "빠르게 아무 사이트나 시도해볼 때", 기존
방식은 "특정 사이트를 정확하고 빠르게 반복 수집할 때"** 쓰는 것으로
구분하면 됩니다.

## 9. 현재 알려진 한계 (실제 납품 전 보완 필요)

- **에러 처리가 약함**: 중간에 인터넷이 끊기거나 사이트 구조가 바뀌면
  프로그램이 그대로 멈추고, 그때까지 모은 데이터도 저장되지 않고
  사라집니다
- **대량 수집에는 느림**: 카테고리 수나 상품 수가 아주 많아지면 시간이
  오래 걸릴 수 있어, 그런 경우 더 빠른 방식(Scrapy 등)으로 다시 만드는
  게 좋습니다

## 10. 자주 헷갈릴 수 있는 용어 정리

| 용어 | 쉬운 설명 |
|---|---|
| 크롤링/스크래핑 | 웹사이트의 정보를 자동으로 읽어와 모으는 작업 |
| Python(파이썬) | 이 프로그램을 만든 프로그래밍 언어. 실행하려면 컴퓨터에 설치돼 있어야 함 |
| 라이브러리 | 남이 미리 만들어둔 기능 모음. `pip install`로 설치해서 가져다 씀 |
| CSV | 표 데이터를 저장하는 가장 단순한 파일 형식 (메모장으로도 열림) |
| robots.txt | 그 사이트에서 크롤링해도 되는 범위를 적어둔 규칙 파일 |
| API 키 | 그 서비스(여기서는 구글 AI)를 프로그램에서 사용할 수 있게 해주는 비밀번호 같은 값. 절대 다른 사람과 공유하면 안 됨 |
| .env 파일 | API 키처럼 남에게 보여주면 안 되는 값을 따로 모아두는 파일. GitHub에는 올라가지 않도록 설정돼 있음 |
| 프롬프트(Prompt) | AI에게 "무엇을 해달라"고 요청하는 지시문 |
