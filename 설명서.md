# 도서 정보 크롤러 — 처음 보는 사람을 위한 설명서

코딩을 몰라도 이 프로그램이 뭘 하는지, 어떻게 실행하는지, 결과물이 뭔지
이해할 수 있도록 쓴 안내서입니다. 짧은 요약은 [`README.md`](./README.md)를
참고하세요.

---

## 1. 이게 뭔가요?

**"크롤러"(또는 "스크래퍼")**란 사람이 웹사이트를 하나하나 열어보면서
정보를 복사-붙여넣기 하는 작업을, 프로그램이 대신 자동으로 해주는
도구입니다. 이 프로젝트는 `books.toscrape.com`이라는 **크롤링 연습용으로
공개된 가짜 서점 사이트**에서 책 제목, 가격, 평점, 재고 상태를 자동으로
긁어와 엑셀 파일로 정리해주는 프로그램입니다.

실제 외주에서는 이 구조를 그대로 활용해서, 예를 들어 "경쟁사 쇼핑몰의
상품 가격을 매일 자동으로 모아서 엑셀로 보내주는" 서비스 같은 걸 만들 수
있습니다.

## 2. 파일이 뭐가 있나요?

```
web-crawler-books/
├── scraper.py     ← 크롤링 + 저장 로직 전체 (이 프로그램의 전부)
├── README.md      ← 프로젝트 요약
├── 설명서.md       ← 지금 보고 있는 이 문서
└── .gitignore     ← 실행 결과 파일은 저장소에 올리지 않도록 제외 설정
```

실행하면 `output_books.csv`, `output_books.xlsx`라는 결과 파일이 새로
생기는데, 이 파일들은 저장소에는 포함되어 있지 않고 실행할 때마다 새로
만들어집니다.

## 3. 어떻게 실행하나요?

이 프로그램은 파이썬(Python)으로 만들어져 있어서, 더블클릭으로 바로 열리는
`index.html`과 달리 **컴퓨터에 파이썬이 설치되어 있어야** 실행할 수
있습니다.

1. 파이썬이 설치돼 있는지 확인 (없으면 python.org에서 설치)
2. 아래 명령어로 필요한 부품(라이브러리) 설치 (한 번만 하면 됨)
   ```
   pip install requests beautifulsoup4 xlsxwriter
   ```
3. 이 폴더 위치에서 아래 명령어 실행
   ```
   python scraper.py --pages 3
   ```
4. 실행이 끝나면 같은 폴더에 `output_books.csv`, `output_books.xlsx`
   파일이 생성됨 — 엑셀로 열어서 바로 확인 가능

## 4. 실행하면 내부에서 무슨 일이 일어나나요? (쉬운 설명)

사람이 직접 한다면 이런 순서로 하게 될 작업을, 프로그램이 몇 초 만에
대신 해줍니다.

1. `books.toscrape.com` 사이트의 1페이지 목록 화면을 인터넷에서 가져옴
2. 그 화면 안에서 책 하나하나의 "제목", "가격", "재고 상태", "평점"이
   적힌 부분만 정확히 찾아서 뽑아냄
3. 다음 페이지로 넘어가서 같은 작업 반복 (`--pages` 옵션으로 지정한
   페이지 수만큼)
4. 사이트에 부담을 주지 않도록 페이지마다 0.5초씩 쉬었다가 다음 페이지로
   이동
5. 다 모은 정보를 CSV 파일(메모장으로도 열리는 단순 표 형식)과 Excel
   파일(색깔·굵은 글씨 등 서식이 적용된 표) 두 가지로 저장

> 참고: 이 프로그램을 처음 만들었을 때는 특정 기호(£)가 포함된 가격을
> 숫자로 바꾸는 과정에서 인코딩 오류로 실행이 아예 안 되는 문제가 있었는데,
> 지금은 수정되어 정상적으로 동작합니다.

## 5. 결과물(엑셀 파일)은 어떻게 생겼나요?

| 도서명 | 가격(GBP) | 평점 | 재고 상태 | 페이지 |
|---|---|---|---|---|
| A Light in the Attic | 51.77 | 3 | In stock | 1 |
| Tipping the Velvet | 53.74 | 1 | In stock | 1 |

- 맨 위 줄(제목 줄)은 진한 배경색으로 강조되어 있고, 스크롤해도 항상
  보이도록 고정되어 있습니다
- 가격 열은 통화 기호가 자동으로 붙는 서식이 적용돼 있습니다

## 6. 실행 옵션 바꾸는 법

명령어 뒤에 옵션을 붙여서 동작을 조절할 수 있습니다.

| 옵션 | 뜻 | 예시 |
|---|---|---|
| `--pages` | 몇 페이지까지 수집할지 (기본 3페이지) | `python scraper.py --pages 5` |
| `--csv` | CSV 파일 저장 경로/이름 지정 | `python scraper.py --csv 결과.csv` |
| `--excel` | Excel 파일 저장 경로/이름 지정 | `python scraper.py --excel 결과.xlsx` |

## 7. 다른 사이트에도 이 프로그램을 쓸 수 있나요?

네, 하지만 **사이트마다 화면 구조가 달라서 코드를 그 사이트에 맞게 고쳐야
합니다.** 지금 코드는 `books.toscrape.com`이라는 특정 사이트의 화면
구조(어느 위치에 제목이 있고, 어느 위치에 가격이 있는지)에 맞춰져
있습니다. 실제 클라이언트의 사이트에 쓰려면 그 사이트 구조를 분석해서
같은 방식으로 다시 맞춰야 합니다.

⚠️ **크롤링하기 전에 반드시 확인할 것**: 아무 사이트나 마음대로
크롤링하면 안 됩니다. 대상 사이트의 이용약관이나 `robots.txt`(크롤링
허용 범위를 적어둔 파일)를 먼저 확인하고, 로그인이 필요한 개인정보나
저작권이 있는 콘텐츠는 수집 대상에서 제외해야 합니다.

## 8. 현재 알려진 한계 (실제 납품 전 보완 필요)

- **에러 처리가 약함**: 중간에 인터넷이 끊기거나 사이트 구조가 바뀌면
  프로그램이 그대로 멈추고, 그때까지 모은 데이터도 저장되지 않고
  사라집니다
- **대량 수집에는 느림**: 페이지 수가 아주 많아지면(수백 페이지 이상)
  시간이 오래 걸릴 수 있어, 그런 경우 더 빠른 방식(Scrapy 등)으로 다시
  만드는 게 좋습니다

## 9. 자주 헷갈릴 수 있는 용어 정리

| 용어 | 쉬운 설명 |
|---|---|
| 크롤링/스크래핑 | 웹사이트의 정보를 자동으로 읽어와 모으는 작업 |
| Python(파이썬) | 이 프로그램을 만든 프로그래밍 언어. 실행하려면 컴퓨터에 설치돼 있어야 함 |
| 라이브러리 | 남이 미리 만들어둔 기능 모음. `pip install`로 설치해서 가져다 씀 |
| CSV | 표 데이터를 저장하는 가장 단순한 파일 형식 (메모장으로도 열림) |
| robots.txt | 그 사이트에서 크롤링해도 되는 범위를 적어둔 규칙 파일 |
